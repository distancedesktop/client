{
  "name": "distance-client",
  "private": true,
  "version": "0.2.0",
  "description": "Remote Desktop Client for Distance (pure web)",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vite": "^8.0.0"
  }
}
